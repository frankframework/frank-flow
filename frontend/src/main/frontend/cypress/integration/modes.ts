describe('Test the different modes', function () {
  // TODO: Check if all modes can be switched to via the mode-switcher.
  // TODO: Check if default-mode-setting in settings works.
  // TODO: Check if last-mode-setting in settings works.
});
